[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pneumonia-cnn"
version = "0.1.0"
description = "Détection de pneumonie sur radiographies thoraciques (DenseNet121 / TorchXRayVision)"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["pneumonia"]